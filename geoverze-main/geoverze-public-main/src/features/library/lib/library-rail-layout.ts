/** Shared dimensions for GEOlibrary horizontal rail cards. */
export const libraryRailCardClass =
  "h-[22.5rem] w-[min(82vw,18rem)] shrink-0 snap-start sm:w-[18rem]";

export const libraryRailMediaClass = "aspect-[16/10] shrink-0 overflow-hidden";

/** Compact content-sized category filter chips — no fixed width/height. */
export const libraryCategoryTileClass = "shrink-0 snap-start whitespace-nowrap";

/** Solid opaque bronze-metal GEOlibrary category filter button. */
export const libraryCategoryFilterClass =
  "library-category-metal inline-flex h-10 items-center gap-2 rounded-full px-4 text-xs font-light tracking-tight focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-bronze/50 sm:h-11 sm:gap-2.5 sm:px-5 sm:text-sm";

/** Active/selected category filter — brighter polished bronze. */
export const libraryCategoryFilterActiveClass = "library-category-metal-active";

export const libraryCategoryFilterIconClass = "library-category-metal-icon";

export const libraryCategoryFilterLabelClass = "library-category-metal-label";
